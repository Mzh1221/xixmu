**11 . (单选题) 执行下面操作后，list2的值是（  ）** 
```python
list1 = ['a', 'b', 'c']  
list2 = list1  
list1.append('de')
#输出['a','b','c','de']
```
这个涉及到深拷贝和浅拷贝，直接'='是深拷贝，直接把地址拷过去。
浅拷贝是只拷贝值, 避免共享内存。
```python
list2 = list1[:]
list2 = list1.copy()
```