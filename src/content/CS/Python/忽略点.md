右闭好像只有 random.randint

素数判断范围可以只到 `int (n**0.5)+1`，因为超了根号 n 后面两个数相乘一定更大

range 传参需要整数，可以用 int，会直接抛弃小数部分

判断素数感觉挺重要的，好好练习一下

快速解决字典找值最大要求知道 key：maxName=max(dic,key=dic.get)

列表排序：lst. sort (key=lambda x: x[1], reverse=True) 这样值最大的就在最前面
不想改原列表就 newLst=sorted (lst, key=lambda x: x[1], reverse=True)

列表. append () 只能一次添加一个元素，如果要加一组的话要把两个数据打包成元组

注意 lst. sort 不会返回值，也就是我要缩减在 for i in ... 一起时 in 后面实际上是空，因为. sort 不会返回值，应当用 sorted ()

单行 if else 写法: x/y if y!=0 else "divided by zero!" 

字典里如果是运算那么在字典创建时就会直接算出来，因此要把会报错的/0 提前判断掉
```Python
m=float(input())
x=input()
n=float(input())
if x=="/" and n==0:
    print("divided by zero!")
else:
    result_dist={"+":m+n,"-":m-n,"*":m*n,"/":m/n}
    if x not in result_dist:
        print("input operater error")
    else:
        print(result_dist[x])
```